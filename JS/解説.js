if (条件) {
    // 条件が真（ture）であれば、実行する
  }

// 等価性
  if (A == B){
    // AとBの値が同じだったら実行
  }

// 比較演算子
  if (A > B){     // ①
    // AよりBが大きかったら実行
  }
   
  if (A >= B){    // ②
    // AがB以上だったら実行
  }


// 論理演算子
  if ((条件A)&&(条件B)){
    // 条件Aと条件Bが満たされたら実行
  }